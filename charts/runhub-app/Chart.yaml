apiVersion: v2
name: runhub-app
version: 0.0.0
dependencies:
  - name: dev
    version: 0.0.0
    condition: global.environment.dev,defaults.dependencies.enabled
  - name: prod
    version: 0.0.0
    condition: global.environment.prod,defaults.dependencies.enabled
