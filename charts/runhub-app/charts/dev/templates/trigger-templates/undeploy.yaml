apiVersion: triggers.tekton.dev/v1alpha1
kind: TriggerTemplate
metadata:
  name: undeploy
spec:
  params:
    - name: service-tag
  resourcetemplates:
    - apiVersion: tekton.dev/v1beta1
      kind: PipelineRun
      metadata:
        generateName: undeploy-$(tt.params.service-tag)-
      spec:
        serviceAccountName: undeploy
        pipelineRef:
          name: undeploy
        params:
          - name: service-tag
            value: $(tt.params.service-tag)
