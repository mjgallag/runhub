apiVersion: triggers.tekton.dev/v1alpha1
kind: TriggerTemplate
metadata:
  name: undeploy
spec:
  params:
    - name: route-tag
  resourcetemplates:
    - apiVersion: tekton.dev/v1beta1
      kind: PipelineRun
      metadata:
        generateName: undeploy-$(tt.params.route-tag)-
      spec:
        pipelineRef:
          name: undeploy
        params:
          - name: route-tag
            value: $(tt.params.route-tag)
