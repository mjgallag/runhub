apiVersion: tekton.dev/v1beta1
kind: Pipeline
metadata:
  name: build-deploy
spec:
  params:
    - name: app-git-url
      type: string
  workspaces:
    - name: src
  tasks:
    - name: get-runhub-src
      taskRef:
        name: git-clone
      params:
        - name: url
          value: https://github.com/mjgallag/runhub.git
        - name: subdirectory
          value: runhub
      workspaces:
        - name: output
          workspace: src
    - name: get-app-src
      taskRef:
        name: git-clone
      params:
        - name: url
          value: $(params.app-git-url)
        - name: subdirectory
          value: app
      workspaces:
        - name: output
          workspace: src
