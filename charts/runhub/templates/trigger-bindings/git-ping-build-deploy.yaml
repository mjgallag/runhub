apiVersion: triggers.tekton.dev/v1alpha1
kind: TriggerBinding
metadata:
  name: git-ping-build-deploy
spec:
  params:
    - name: git-revision
      value: $(body.repository.default_branch)
    - name: route-tag
      value: $(body.repository.default_branch)
    - name: image-tag
      value: $(body.repository.default_branch)
    - name: cache-from-image-tag
      value: $(body.repository.default_branch)
