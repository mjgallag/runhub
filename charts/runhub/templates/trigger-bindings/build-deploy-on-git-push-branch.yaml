apiVersion: triggers.tekton.dev/v1alpha1
kind: TriggerBinding
metadata:
  name: build-deploy-on-git-push-branch
spec:
  params:
    - name: git-revision
      value: $(extensions.branch)
    - name: route-tag
      value: $(extensions.branch)
    - name: image-tag
      value: $(extensions.branch)
    - name: cache-from-image-tag
      value: $(extensions.cache-from-image-tag)
